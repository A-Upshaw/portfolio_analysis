name: finance_platform
version: '1.0'
profile: finance_platform

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  finance_platform:
    staging:
      +materialized: view
    marts:
      +materialized: table
